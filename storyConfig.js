// Story Map Configuration — seed file for the Story Creator editor
// Edit chapters using the editor UI and use Export to download an updated version.

const storyConfig = {
  chapters: [],
};

if (typeof module !== 'undefined' && module.exports) {
  module.exports = storyConfig;
}
